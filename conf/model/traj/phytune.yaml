name: PhyTune
logdir: "logdir/traj_train/${model.name}/${dataset.name}" # logdir/${model_name}/${dataset name}/run{x}/
continue_training: False

args:
  - none
  - none

kwargs:
  input_dim: -1
  output_dim: -1
  
seed: 42
num_epochs: 20
lr_init: 5e-1
lr_gamma: 0.95
lr_step_size: 10
training_data_split: 0.8
batch_trajectory: 20
estimation_fraction: 0.25

valid_interval: 5