model_name: physics_optim
model_path: data/model/physics_optim
logdir: logdir/physics_optim # logdir/${model_name}/${dataset name}/run{x}/
continue_training: False

seed: 42
num_epochs: 10
lr_init: 1e-2
lr_gamma: 0.95
lr_step_size: 10
training_data_split: 0.8
batch_trajectory: 20
estimation_fraction: 0.15

use_data: 0.35